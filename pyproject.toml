[project]
name = "hackathon-qa-automation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shoaib1207k", email = "shoaib1207k@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
    "strands-agents[openai]>=1.56.0",
]

[project.scripts]
hackathon-qa-automation = "hackathon_qa_automation.cli:main"

[build-system]
requires = ["uv_build>=0.12.16,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
